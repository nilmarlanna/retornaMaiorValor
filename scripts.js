// Escrever uma função que usa 2 numeros e retorna o maior entre eles

letvalorMaior = max(10,10);
console.log(valorMaior);

function max(numero1, numero2) {
    if(numero1 > numero2)
    return numero1;
else return numero2;
}
